/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "xat95hp7xqg7aqmn.public.blob.vercel-storage.com",
      },
    ],
  },
};

export default nextConfig;
